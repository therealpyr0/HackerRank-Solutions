#include <map>
#include <set>
#include <list>
#include <cmath>
#include <ctime>
#include <deque>
#include <queue>
#include <stack>
#include <string>
#include <bitset>
#include <cstdio>
#include <limits>
#include <vector>
#include <climits>
#include <cstring>
#include <cstdlib>
#include <fstream>
#include <numeric>
#include <sstream>
#include <iostream>
#include <algorithm>
#include <unordered_map>

using namespace std;


int main(){
    int q,d1,d2;
    cin >> q;
    for(int a0 = 0; a0 < q; a0++){
        int x;
        int y;
        int z;
        cin >> x >> y >> z;
    d1=abs(x-z);
    d2=abs(y-z);
    if(d2==d1)cout<<"Mouse C"<<endl;
    else if(d2<d1)cout<<"Cat B"<<endl;
    else cout<<"Cat A"<<endl;
    }
    return 0;
}
